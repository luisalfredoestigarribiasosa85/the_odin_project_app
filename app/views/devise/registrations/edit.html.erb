<div class="min-h-screen bg-gradient-to-tr from-indigo-50 via-slate-100 to-indigo-100 flex items-center justify-center py-12 px-2 sm:px-6 lg:px-8">
  <div class="w-full max-w-2xl space-y-12 bg-white p-10 rounded-3xl shadow-2xl border border-slate-200">
    <div class="flex flex-col items-center space-y-3">
      <div class="w-16 h-16 rounded-full bg-indigo-100 flex items-center justify-center mb-2 shadow-inner">
        <svg class="w-10 h-10 text-indigo-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M12 4a4 4 0 0 1 4 4v3a4 4 0 0 1-8 0V8a4 4 0 0 1 4-4z"/>
          <path d="M20 21v-2a4 4 0 0 0-3-3.87"/>
          <path d="M4 21v-2a4 4 0 0 1 3-3.87"/>
        </svg>
      </div>
      <h2 class="text-4xl font-extrabold tracking-tight text-indigo-700">Actualizar cuenta</h2>
      <p class="text-base text-slate-500 mt-1">Edita tu información de perfil y tus credenciales fácilmente.</p>
    </div>

    <div class="space-y-4">
      <%= render "devise/shared/error_messages", resource: resource %>
    </div>

    <%= form_for(resource,
                 as: resource_name,
                 url: registration_path(resource_name),
                 html: { method: :put, class: "space-y-10" }) do |f| %>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <div class="space-y-6">
          <div>
            <%= f.label :email, "Correo electrónico", class: "block text-sm font-medium text-indigo-600" %>
            <%= f.email_field :email,
                  autofocus: true,
                  autocomplete: "email",
                  class: "mt-2 block w-full rounded-xl border border-indigo-200 bg-white px-4 py-2.5 text-slate-900 shadow focus:border-indigo-400 focus:ring-2 focus:ring-indigo-300 transition" %>
          </div>

          <div>
            <%= f.label :username, "Nombre de usuario", class: "block text-sm font-medium text-indigo-600" %>
            <%= f.text_field :username,
                  autocomplete: "username",
                  class: "mt-2 block w-full rounded-xl border border-indigo-200 bg-white px-4 py-2.5 text-slate-900 shadow focus:border-indigo-400 focus:ring-2 focus:ring-indigo-300 transition" %>
          </div>

          <div>
            <%= f.label :bio, "Biografía", class: "block text-sm font-medium text-indigo-600" %>
            <%= f.rich_text_area :bio,
                  rows: 3,
                  class: "mt-2 block w-full rounded-xl border border-indigo-200 bg-white px-4 py-2.5 text-slate-900 shadow focus:border-indigo-400 focus:ring-2 focus:ring-indigo-300 transition" %>
          </div>
        </div>

        <div class="space-y-6">
          <div>
            <%= f.label :avatar, "Avatar", class: "block text-sm font-medium text-indigo-600" %>
            <%= f.file_field :avatar,
                  class: "mt-2 block w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:font-semibold file:bg-indigo-50 file:text-indigo-700 hover:file:bg-indigo-100 transition" %>
          </div>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div class="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800 mb-2">
              Actualmente esperando confirmación para: <strong><%= resource.unconfirmed_email %></strong>
            </div>
          <% end %>

          <div>
            <div class="flex flex-col gap-1 sm:flex-row sm:items-center sm:justify-between">
              <%= f.label :password, "Contraseña", class: "block text-sm font-medium text-indigo-600" %>
              <span class="text-xs text-slate-500">Déjalo en blanco si no deseas cambiarla.</span>
            </div>
            <%= f.password_field :password,
                  autocomplete: "new-password",
                  class: "mt-2 block w-full rounded-xl border border-indigo-200 bg-white px-4 py-2.5 text-slate-900 shadow focus:border-indigo-400 focus:ring-2 focus:ring-indigo-300 transition" %>
            <% if @minimum_password_length %>
              <p class="mt-2 text-xs text-slate-500">Mínimo <%= @minimum_password_length %> caracteres.</p>
            <% end %>
          </div>

          <div>
            <%= f.label :password_confirmation, "Confirmar contraseña", class: "block text-sm font-medium text-indigo-600" %>
            <%= f.password_field :password_confirmation,
                  autocomplete: "new-password",
                  class: "mt-2 block w-full rounded-xl border border-indigo-200 bg-white px-4 py-2.5 text-slate-900 shadow focus:border-indigo-400 focus:ring-2 focus:ring-indigo-300 transition" %>
          </div>

          <div>
            <div class="flex flex-col gap-1 sm:flex-row sm:items-center sm:justify-between">
              <%= f.label :current_password, "Contraseña actual", class: "block text-sm font-medium text-indigo-600" %>
              <span class="text-xs text-slate-500">Necesitamos tu contraseña actual para confirmar los cambios.</span>
            </div>
            <%= f.password_field :current_password,
                  autocomplete: "current-password",
                  class: "mt-2 block w-full rounded-xl border border-indigo-200 bg-white px-4 py-2.5 text-slate-900 shadow focus:border-indigo-400 focus:ring-2 focus:ring-indigo-300 transition" %>
          </div>
        </div>
      </div>
      <div class="flex justify-end pt-4">
        <%= f.submit "Guardar cambios",
            class: "inline-flex items-center gap-2 py-2.5 px-8 rounded-xl bg-gradient-to-r from-indigo-600 to-indigo-500 text-white text-base font-semibold shadow-md transition hover:from-indigo-700 hover:to-indigo-600 focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:ring-offset-1" %>
      </div>
    <% end %>

    <div class="space-y-3 rounded-2xl border border-red-200 bg-gradient-to-br from-red-50 to-white p-6 shadow">
      <h3 class="text-base font-semibold text-red-700 flex items-center gap-2">
        <svg class="w-5 h-5 text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path d="M6 18L18 6M6 6l12 12" />
        </svg>
        Cancelar mi cuenta
      </h3>
      <p class="text-sm text-red-600">Si ya no deseas utilizar el servicio, puedes eliminar tu cuenta de forma permanente.</p>
      <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between mt-3">
        <%= button_to "Eliminar cuenta", registration_path(resource_name),
                      method: :delete,
                      data: { confirm: "¿Estás seguro?", turbo_confirm: "¿Estás seguro?" },
                      class: "inline-flex items-center gap-2 justify-center rounded-xl border border-red-200 bg-white px-5 py-2.5 text-sm font-semibold text-red-700 shadow hover:border-red-400 hover:bg-red-50 transition cursor-pointer" %>
        <%= link_to "Volver", cars_path,
                      class: "inline-flex items-center gap-2 justify-center rounded-xl border border-slate-200 bg-white px-5 py-2.5 text-sm font-semibold text-slate-700 shadow hover:border-indigo-200 hover:bg-indigo-50 transition cursor-pointer" %>
      </div>
    </div>
  </div>
</div>
