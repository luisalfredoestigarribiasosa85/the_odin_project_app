<% content_for :title, "Inicio" %>

<div class="space-y-24">
  <section class="relative overflow-visible rounded-3xl border border-slate-200 bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 text-white shadow-2xl">
    <div class="pointer-events-none absolute inset-0 overflow-hidden rounded-3xl">
      <div class="absolute inset-y-0 right-0 hidden w-2/5 bg-[radial-gradient(circle_at_top,_rgba(37,99,235,0.4),_transparent_60%)] sm:block"></div>
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_left,_rgba(16,185,129,0.35),_transparent_60%)]"></div>
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_bottom,_rgba(15,118,110,0.35),_transparent_65%)]"></div>
      <div class="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-white/30 to-transparent"></div>
    </div>

    <div class="relative z-10 mx-auto flex flex-col gap-10 px-6 py-16 xl:flex-row xl:items-center lg:max-w-[1200px]">
      <div class="space-y-6 text-balance">
        <p class="inline-flex items-center gap-2 rounded-full border border-white/30 px-4 py-2 text-xs font-semibold uppercase tracking-[0.3em] text-emerald-100">
          <span class="text-base">★</span> Boutique automotriz
        </p>
        <div class="space-y-4">
          <h1 class="text-4xl font-bold leading-tight sm:text-5xl">
            Tu próximo vehículo en un solo clic
          </h1>
          <p class="text-base text-slate-100/80">
            Nos especializamos en la venta de vehículos de todas las marcas y modelos, nuevos y de segunda mano, con asesoramiento personalizado y garantías de calidad.
          </p>
        </div>
        <div class="flex flex-col gap-3 sm:flex-row">
          <%= link_to "Ver catálogo completo", cars_path,
                      class: "inline-flex items-center justify-center rounded-full bg-white px-6 py-3 text-sm font-semibold text-slate-900 shadow-lg shadow-white/20 transition hover:-translate-y-0.5 hover:shadow-xl" %>
          <% secondary_cta_path = user_signed_in? ? new_car_path : new_user_registration_path %>
          <%= link_to(user_signed_in? ? "Publicar mi auto" : "Crear cuenta",
                      secondary_cta_path,
                      class: "inline-flex items-center justify-center rounded-full border border-white/40 px-6 py-3 text-sm font-semibold text-white/90 transition hover:-translate-y-0.5 hover:border-white hover:text-white") %>
        </div>
        <div class="flex flex-col gap-4 border-t border-white/20 pt-6 text-sm text-slate-200 sm:flex-row sm:items-center">
          <div class="flex items-center gap-3">
            <span class="inline-flex h-10 w-10 items-center justify-center rounded-2xl bg-white/15 text-lg">🚗</span>
            <div>
              <p class="font-semibold">Entrega certificada</p>
              <p>Revisión mecánica completa + documentación al día.</p>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <span class="inline-flex h-10 w-10 items-center justify-center rounded-2xl bg-white/15 text-lg">🤝</span>
            <div>
              <p class="font-semibold">Asesores reales</p>
              <p>Resolvemos dudas por videollamada o presencial.</p>
            </div>
          </div>
        </div>
      </div>

      <div class="grid grid-cols-2 gap-5 sm:grid-cols-2 sm:gap-7 lg:grid-cols-4 xl:grid-cols-2 xl:max-w-sm xl:self-stretch lg:max-w-[400px]">
        <% [[User.count, "Clientes felices"], [Car.count, "Autos activos"], ["24/7", "Soporte dedicado"], ["4.9/5", "Calificación"]].each do |metric, label| %>
          <div class="rounded-[28px] border border-white/60 bg-gradient-to-br from-white via-white/90 to-slate-50/80 px-5 py-7 text-center shadow-2xl shadow-black/15 content-center">
            <p class="text-3xl font-bold text-slate-900"><%= metric %></p>
            <p class="mt-2 text-xs font-semibold uppercase tracking-[0.25em] text-slate-500"><%= label %></p>
          </div>
        <% end %>
      </div>
    </div>
  </section>

  <section class="grid gap-10 rounded-3xl border border-slate-200 bg-white/95 p-8 shadow-xl shadow-slate-200/60 lg:grid-cols-[minmax(0,1.1fr)_minmax(0,1fr)]">
    <div class="space-y-5">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">Autos disponibles</p>
      <h2 class="text-3xl font-bold text-slate-900">Compra y vende autos de manera segura</h2>
      <p class="text-base text-slate-600">
        Publica tus autos y encuentra autos para comprar de manera fácil y segura.
      </p>
      <div class="mt-5 grid gap-4 sm:grid-cols-2">
        <% features = [
          ["Publica tu auto", "Publica tus autos y encuentra compradores"],
          ["Compradores de confianza", "Compradores registrados y revisados"],
          ["Coordinar pagos por teléfono", "Coordinar pagos por teléfono directamente con los compradores"],
          ["Asistencia en línea", "Atención al cliente 24/7"]
        ] %>
        <% features.each do |title, description| %>
          <div class="rounded-2xl border border-slate-100 bg-slate-50/80 p-5">
            <p class="text-base font-semibold text-slate-900"><%= title %></p>
            <p class="mt-1 text-sm text-slate-600"><%= description %></p>
          </div>
        <% end %>
      </div>
    </div>

    <div class="space-y-6 rounded-3xl border border-slate-100 bg-slate-900/90 p-8 text-white shadow-lg shadow-slate-900/20">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-white/50">Comienza ahora</p>
      <h3 class="text-2xl font-bold">Registrate y comienza a publicar tus autos</h3>
      <div class="mt-5 grid gap-4 sm:grid-cols-2">
        <% steps = [
          ["Registrarte", "Crea tu cuenta de usuario y sube tus datos"],
          ["Publica tu auto", "Carga información detallada de tu auto y comienza a vender"],
          ["Comprar autos", "Busca autos disponibles y contacta a los vendedores"],
          ["Coordinar pagos por teléfono", "Coordinar pagos por teléfono directamente con los compradores"]
        ] %>
        <% steps.each_with_index do |(title, description), index| %>
          <div class="flex gap-4">
            <div class="mt-1 flex h-10 w-10 items-center justify-center rounded-full bg-white/15 text-sm font-bold text-white/90">
              <%= index + 1 %>
            </div>
            <div>
              <p class="text-base font-semibold"><%= title %></p>
              <p class="text-sm text-white/80"><%= description %></p>
            </div>
          </div>
        <% end %>
      </div>
      <%= link_to "Registrarse", new_user_registration_path,
                  class: "inline-flex items-center justify-center rounded-full bg-white px-5 py-3 text-sm font-semibold text-slate-900 shadow-lg shadow-white/20 transition hover:-translate-y-0.5" %>
    </div>
  </section>

  <section class="rounded-3xl border border-slate-200 bg-gradient-to-br from-white via-slate-50 to-slate-100 p-8 shadow-lg">
    <div class="flex flex-col gap-6 sm:flex-row sm:items-end sm:justify-between">
      <div>
        <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">Destacados</p>
        <h2 class="mt-2 text-3xl font-bold text-slate-900">Vehículos listos para estrenar</h2>
        <p class="mt-3 max-w-2xl text-base text-slate-600">Explora algunas opciones populares que se encuentran disponibles ahora mismo.</p>
      </div>
      <%= link_to "Ver todos los autos", cars_path,
                  class: "inline-flex items-center justify-center rounded-full border border-slate-300 bg-white px-5 py-2.5 text-sm font-semibold text-slate-700 shadow-sm transition hover:-translate-y-0.5 hover:border-slate-400 hover:text-slate-900 hover:shadow-lg" %>
    </div>

    <% if @featured_cars.any? %>
      <div class="mt-10 grid gap-8 md:grid-cols-2 xl:grid-cols-3">
        <%= render partial: "cars/car", collection: @featured_cars, as: :car, locals: { context: :card } %>
      </div>
    <% else %>
      <div class="mt-10 rounded-2xl border border-dashed border-slate-300 bg-white/80 px-6 py-8 text-center text-slate-500">
        Todavía no hay vehículos publicados. Sé la primera persona en agregar uno.
      </div>
    <% end %>
  </section>

  <section class="rounded-3xl border border-slate-200 bg-white/95 p-8 shadow-lg">
    <div class="flex flex-col gap-6">
      <div>
        <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">Revisa nuestros clientes satisfechos</p>
        <h2 class="mt-2 text-3xl font-bold text-slate-900">Historias de la satisfacción de los compradores</h2>
      </div>
      <div class="grid gap-5 md:grid-cols-3">
        <% testimonials = [
          ["“Llegué a mi casa sin problemas y me entregaron el auto en perfectas condiciones.”", "María, Asunciòn"],
          ["“Puedo recomendarte a mis amigos y seguro que se comprarán un auto con tu servicio.”", "Juan, San Lorenzo"],
          ["“Me gustó mucho la facilidad de contactar a los vendedores, era muy fácil.”", "Ana, Capiatá"]
        ] %>
        <% testimonials.each do |quote, name| %>
          <div class="flex flex-col gap-4 rounded-2xl border border-slate-100 bg-slate-50/80 p-5">
            <span class="text-3xl text-slate-900/50">“</span>
            <p class="text-base text-slate-700"><%= quote %></p>
            <p class="text-sm font-semibold text-slate-900"><%= name %></p>
          </div>
        <% end %>
      </div>
    </div>
  </section>

  <section class="rounded-3xl border border-slate-200 bg-white/95 p-8 text-center shadow-xl shadow-slate-200/70">
    <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">Atención personalizada</p>
    <h2 class="mt-2 text-3xl font-bold text-slate-900">¿Listo para comprar tu próximo auto?</h2>
    <p class="mt-3 text-base text-slate-600">
      Agenda una visita a un vendedor registrado, revisa los autos disponibles y compra sin complicaciones.
    </p>
    <div class="mt-6 flex flex-col items-center justify-center gap-4 sm:flex-row">
      <%= link_to "Explorar catálogo", cars_path,
                  class: "inline-flex items-center justify-center rounded-full border border-slate-300 px-6 py-3 text-sm font-semibold text-slate-700 shadow-sm transition hover:shadow-slate-200 hover:scale-105" %>
      <%= link_to "Registrarse", new_user_registration_path,
                  class: "inline-flex items-center justify-center rounded-full border border-slate-300 px-6 py-3 text-sm font-semibold text-slate-700 shadow-sm transition hover:shadow-slate-200 hover:scale-105" %>
    </div>
  </section>
</div>
